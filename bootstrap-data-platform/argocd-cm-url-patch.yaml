# Patch for the argocd-cm ConfigMap
apiVersion: v1
kind: ConfigMap
metadata:
  name: argocd-cm
data:
  # Set the external URL so ArgoCD knows how it's being accessed
  url: https://argocd.upcloud.playground.dataminded.cloud
